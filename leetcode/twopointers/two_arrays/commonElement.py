"""
You are given two sorted arrays:

👉 A = [1, 2, 4, 5, 6]
👉 B = [2, 4, 6, 8]

Task:

Find all numbers that appear in both arrays

Thinking:

One pointer for each array
Compare current elements:
If equal → keep it, move both
If A is smaller → move A pointer
If B is smaller → move B pointer
"""

def common_element(lstA, lstB):
    for p1 in range(len(lstA)):
        ...
    for p2 in range(len(lstB)):
        ...


common_element()